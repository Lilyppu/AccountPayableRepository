server:
  port: 8902
  servlet:
    context-path: /ap-svc
spring:
  datasource:
    url: jdbc:postgresql://10.10.2.92:5432/postgres
    username: postgres
    password: PPU.P4ssw0rd
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        default_schema: AccountPayable
        dialect: org.hibernate.dialect.PostgreSQLDialect

